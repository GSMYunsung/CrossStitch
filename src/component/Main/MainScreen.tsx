import React from 'react';
import { Octokit } from 'octokit'
import { octokit } from '../../network/octokit';

export default function MainScreen() {

    return (
        <div>
            <button onClick={()=>{}}>Get Repository</button>
        </div>
    );
}